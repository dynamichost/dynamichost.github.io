{
  "name": "ip-api-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Simple JSON-backed IP API gateway with web management UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
